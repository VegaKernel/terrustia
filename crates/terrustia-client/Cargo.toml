[package]
name = "terrustia-client"
# A headless client for testing and bots. Useful, but shaped for this repository's own probes rather than as a public API to keep stable.
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "A headless Terraria client: handshake, world view, and scripted actions"

[dependencies]
terrustia-proto = { path = "../terrustia-proto" }

bytes.workspace = true
thiserror.workspace = true
tokio.workspace = true
tokio-util.workspace = true
tracing.workspace = true

[dev-dependencies]
flate2.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[lints]
workspace = true
